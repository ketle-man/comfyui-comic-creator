# 作業計画: 「Auto」タブ（AIによるマンガ自動作成）の追加

作成日: 2026-09-21 / 改訂: 2026-09-25（脚本パースの頑健化・レイアウトGenerateモーダルとのAuto連携を追記、§12-10）
ステータス: **第1弾（ストーリー・脚本タブ）・第2弾（オートレイアウトサブタブ）とも実装完了・実機確認済み・未リリース**（ブランチ `feature/auto-tab`）。詳細は DEVLOG 2026-09-21・2026-09-22・2026-09-24・2026-09-25 参照。第2弾の詳細は本ファイル §12、追加改善は §12-8、バグ修正は §12-9、追加機能・頑健化は §12-10。
- 第1弾: §7のステップ1〜9をすべて実施。ローカルLLM（Ollama）・Gemini（テキスト/画像）・ローカル画像生成（Workflow Studio経由）を実機確認済み。Unsloth中継はユーザーがUnslothのモデルで動作確認済み（Ollamaモデル指定時のエラーはChromeの設定リセットで解消、2026-09-22）。ホスト名接続失敗の問題もChromeの設定リセットで改善済み。
- 第2弾: §12参照。コマ割り自動生成・レイアウトタブへの転送を実装し実機確認済み。**サブタブ名は「レイアウト」ではなく「オートレイアウト」に確定**（既存のレイアウトタブと区別するためユーザー指定、2026-09-22）。以下、本ファイル中の第1弾計画時点の「レイアウトサブタブ」という表記は執筆時点の仮称であり、実装後の正式名称は「オートレイアウト」である。
- **2026-09-24、ユーザーフィードバックにより追加改善8件を実施（§12-8）**: フォントタブ連携（既定フォントに設定ボタン）、コマ選択プロパティのUI調整、転送時のz-orderバグ修正、コマ間余白と外側余白の独立化（2段階）、生成のランダム性、ツールボタンのUI改善、作成言語のUI設定言語対応、ヘルプ反映。
- **2026-09-25、作成言語対応（上記8の7）に2件のバグ修正（§12-9）**: 見出し記号の言語混在（英語UIで「# 【Title】」のような出力になる）、「サンプル」ボタンが日本語固定だった問題。ともに実機確認済み。
- **2026-09-25、続けて脚本パースの頑健化とレイアウトGenerate連携を実施（§12-10）**: LLM出力の閉じ括弧不足を自動修復するJSON修復ステップの追加、レイアウトタブ「Generate」モーダルに「Autoから取得」ボタンを新設（ストーリー・脚本の`action`をI2I/T2Iのプロンプトへ反映、転送機能とは独立）。
- ヘルプ（ja/en/zh）・README（3言語）は第1弾・第2弾（オートレイアウト全体）・§12-8・§12-9・§12-10とも**すべて反映済み**（2026-09-25時点）。バージョン更新・リリースは未実施（明示指示があるまで行わない）。
- **次の段階**: §12-7「残作業」参照（テンプレート対応付けタブ・画像生成連携・一部スタイルが未実機検証。バージョンアップ・リリース）。

**確認点の回答（2026-09-21）による確定事項の要約**
- LLMクライアントは**WFSから必要部分をCCへ移植**（§4-4）。
- 「作成」は**2段階**（ストーリー生成 → 確認・編集 → 脚本生成）（§3-1）。
- Chatは**ストーリー・脚本の編集ができる**ことが要件。ツールはAuto向けの新ツール（脚本化・セリフ推敲など）を予定。WFSの翻訳・VLMは**レイアウトタブ作業後に検討**（§5）。
- 画像生成は**ローカル（WFS経由）/ Gemini をラジオボタンで選択**。各設定は設定タブの「画像生成」欄。生成画像は **`output/cc_auto/`** に保存（§5-3）。

UIモックアップ（ユーザー作成）: [docs/plan/auto_tab_mockup.png](docs/plan/auto_tab_mockup.png)

## 1. 背景・目的

プロトタイプ `now_work/manga_panel`（React + Vite + Express、AI Studio製）は、自動化フローの確認だけを目的にした荒い作りである。
この機能を参考に、Comic Creator（以下CC）へ「マンガの自動作成」を追加する。

**方針（2026-09-21ユーザー決定）**
- 参考アプリのコードは移植せず、**CCの既存機能を活かし、一部を修正する形**で実装する。
- **以前の方針「コマ割りの自動生成は行わない」を撤回する**（AI活用＝自動化を含むため）。
  ただし自動出力そのままでは完成度が足りないため、**「自動作成 → 人が編集して仕上げる」フロー**を前提にする。
- 新規タブ **「Auto」** を追加する。Autoタブは**既存のスクリプトタブとは連携せず、新規作品として作る**（現時点）。

## 2. 全体構成と実施順

Autoタブは2つのサブタブを持つ。**ストーリー・脚本タブを先に完成させてから、レイアウトタブに移る。**

| サブタブ | 内容 | 時期 |
|---|---|---|
| **ストーリー・脚本** | お題からストーリーと脚本をAIで作成・編集。AIチャット付き | **第1弾（今回の計画の対象）** |
| **レイアウト** | 脚本からのコマ割り自動生成（参考アプリの機能2・3）、既存レイアウトタブ/スクリプトタブとの連携 | 第1弾完成後に別途計画 |

参考アプリの機能との対応:

| # | 参考アプリの機能 | CCの現状 | 扱い |
|---|---|---|---|
| 1 | お題＋総ページ数 → 全ページ脚本のLLM生成 | 無い | **第1弾（ストーリー・脚本タブ）** |
| 4 | LLM出力の頑健なパース | 無い | **第1弾の一部** |
| 2 | 重要度に応じたコマサイズ按分 | 無い | レイアウトサブタブで実施 |
| 3 | 再帰分割による自動コマ配置 | 無い | レイアウトサブタブで実施 |
| — | コマ手動分割・落書き・フキダシ・テンプレート・複数ページ・JSON/SVG出力 | CCに既存（より高機能） | 対象外 |

## 3. ストーリー・脚本タブのUI（3ペイン）

```
[Auto] ─ サブタブ: [ストーリー・脚本] [レイアウト]
┌────────────┬───────────────────────┬──────────────────────┐
│ 左ペイン    │ 中央ペイン             │ 右ペイン              │
│ お題入力    │ [ストーリー][脚本][メモ] │ [Chat][設定]          │
│ ページ数指定│  ストーリー/脚本の      │  Chat: AIチャット/TOOLS│
│ [作成]      │  表示・編集            │  設定: AIエンジン設定  │
│ [サンプル]  │  メモ: アイデア記録     │        (ローカル/Gemini)│
└────────────┴───────────────────────┴──────────────────────┘
```

- **左ペイン**: お題・プロンプト入力、ページ数指定、「作成」ボタン、「サンプル」ボタン（LLM未接続でもフローを試せる。参考アプリの「サンプル台本」相当）。

- **中央ペイン**: 3つのタブ。
  - **ストーリー**: 作成したストーリーを表示・編集。
  - **脚本**: 作成した脚本を表示・編集（参考アプリの「各コマの構成・脚本調整」相当。ページ、重要度、下描きガイド、セリフ、フキダシ種別）。
  - **メモ**: アイデアの記録等に自由に使う。
- **右ペイン**: 2つのタブ。
  - **Chat**: Workflow Studio（以下WFS）のAI TOOLタブの **Chat機能とTOOLS機能をまとめたもの**（§5）。
  - **設定**: AIエンジン設定。**ローカル（Ollama等）/ Gemini**（§4）。

### 3-1. 作成フロー（2段階・確定）
1. 左ペイン「作成」→ お題とページ数から**ストーリー**を生成 → 中央「ストーリー」タブに表示。
2. ユーザーがストーリーを確認・編集（手作業、またはChatでの編集）。
3. **編集後のストーリーから脚本を生成** → 中央「脚本」タブに表示（ページ・コマ構造、参考アプリの脚本JSONを土台にする）。
   - 脚本生成の起動ボタンの置き場所（案）: 「脚本」タブ上部の「ストーリーから脚本を作成」ボタン。
   - ストーリーが未生成/空の場合は、ボタンを無効化する（§8で確定）。
- 各段階の生成結果は再生成・手動編集が可能（上書き前に確認ダイアログ）。

## 4. AIエンジン設定（右ペイン「設定」タブ）

### 4-1. 保存先
- **設定内容はCC側に保存する**（WFSの `wfm_ai_settings` は共有しない）。
  → 実装前確認点1は「WFSと**同じ形の入力UI**をCCに置き、CC側に保存」で確定。
- 保存先の候補: ブラウザ `localStorage`（例: `ccc_auto_ai_settings`）。既存のスクリプト作品も localStorage 保存のため同じ方式に揃える。

### 4-2. ローカルLLM（WFS AI TOOL「設定」と同じ項目）
モックアップの設定タブ（WFS AI TOOL設定の再現）:
- **BACKEND**: Ollama / LM Studio / Lemonade / Unsloth
- **CONNECTION**: URL、Test Connection、Unload Model
- **MODEL**: モデル選択、Refresh
- **GENERATION**: Thinking mode、Max tokens
- Save Settings
- **画像生成**（§5-3で詳述）: 生成エンジン（ローカル/Gemini）と、それぞれの設定。WFSの CHAT IMAGE GENERATION / CHAT I2I GENERATION（専用ワークフロー指定）に相当。
- **FREE LANGUAGE**（翻訳ツール用）は、翻訳ツールの導入（レイアウトタブ作業後に検討）まで**設けない**。

### 4-3. Gemini（Nanobananaタブの設定を流用）
- APIキー `NANOBANANA_API_KEY` は `py/ccc.py` が保持しており、**ブラウザからは読めない**。既存のGeminiエンドポイントは**画像生成専用**。
- **`py/ccc.py` にテキスト生成用エンドポイント**（例: `nanobanana/text`）を追加し、同じキー・同じモデル名検証（`_validate_gemini_model`）で `generateContent` を呼ぶ。
- **Geminiのモデル選択は設定タブで行う**（実装前確認点5は確定）。画像モデルはテキスト生成に使えない場合があるため、
  モデル一覧は既存の `nanobanana/models` を流用でき、`generateContent` 対応かつ画像/音声系でない `gemini-*` に絞れることを実機で確認済み（2026-09-21）。
- **Pythonを変更するため、検証時はComfyUIの再起動が必要**。**課金が発生するため、実機での実呼び出しは事前にユーザーへ確認する**。

### 4-4. LLMクライアントの実装方針（確定: CCへ移植）
CCには現在LLMクライアントが**存在しない**（`callLLM` 等はWFS側のみ）。**WFSの `ai-tab.js` から必要部分をCC内の新規ESMモジュールへ移植する**（CC単体で完結し、WFS未導入でも動く。設定をCCに持つ方針とも整合）。

- 移植対象: `callLLM` / `callChat`（ツール呼び出し対応）/ `fetchModels` / `testConnection` / `unloadAiModel` / `_applyGenOptions` / `stripThinkingTags` / `_formatMessagesForBackend` / Unsloth中継 / `isValidBackendUrl`。
  VLM・翻訳用の関数は、翻訳・VLMツールの導入時に追加する。
- WFSと実装が二重になる点は許容する（WFS側は変更しない）。移植元のコミット/日付をモジュール冒頭コメントに残し、将来の同期の手がかりにする。
- ブラウザから直接ローカルLLMへ接続するCORS制約（Ollamaは`OLLAMA_ORIGINS`）はWFSと同じ。
- 既存の橋渡し `_wfmReceiveLLMPromptRequest` は `callLLM` に設定を渡しておらず、思考モード・最大トークンが効かない（既存の不具合候補）。
  新クライアントでは最初から設定を渡す。
- Geminiは、ブラウザからキーを扱えないため`py/ccc.py`経由になる。チャット用に**複数ターンのメッセージ配列とシステムプロンプトを受け取れる**エンドポイントとして設計する。

## 5. Chat機能（右ペイン「Chat」タブ）

WFS AI TOOLタブの Chat機能と TOOLS機能をまとめたものを土台にし、**ストーリー・脚本の編集をChatでできる**ことを要件とする。

### 5-1. WFS側の現状（移植元の把握）
- WFS AI TOOLのサブタブ: Translate / VLM / Chat（スキル管理含む）/ 設定（`app.js`, `i18n.js`の`aiSubTab*`）。
- **Chat**（`ai-tab.js` `initChatTab`）: 会話履歴、画像添付、スキル（システムプロンプト）選択・管理、SVGコードのプレビュー、
  `generate_image` ツール呼び出し（現在ロード中のワークフローでT2I、画像添付時はI2I）。
- **TOOLS**（Translate / VLM）: 翻訳、画像からの説明・プロンプト・タグ・ワイルドカード生成。

### 5-2. CCでの構成
- 右ペイン「Chat」タブに、チャットと**Auto向けツール**を置く。
- **Chatによるストーリー・脚本の編集（要件）**: 現在のストーリー/脚本（編集中の内容）をチャットの文脈としてLLMに渡し、
  修正案を受け取って中央ペインへ反映できるようにする。反映方式は§8で確定（反映ボタン方式）。
- **Auto向けの新ツール（予定）**: 「ストーリーから脚本化」「セリフ推敲」等。ツールは**登録式**（id・表示名・プロンプト生成関数・入力元・反映先）にして、
  後からツールを追加しやすい構造にする。具体的なツール一覧は実装時に詰める。
- **WFSの翻訳・VLMは今回移植しない**（レイアウトタブ作業後に検討）。ただし登録式にしておくことで、後から追加しても手間にならないようにする。
  （ユーザー了承: 「却って手間になる場合は、すべてまとめて追加した上で後で削る」も可。現時点の判断は、まず含めない方が手間が小さい。）
- WFSにある「スキル（システムプロンプト）管理」と「画像添付」は、必要性を実装時に判断（スキルは脚本の文体指定などに有用な可能性）。

### 5-3. 画像生成対応（確定）
- **生成エンジンはラジオボタンで選択**: ローカル（WFS経由） / Gemini（Nanobanana）。
- **各エンジンの設定は、右ペイン設定タブの「画像生成」欄**に置く（Chat側では選択のみ）。
  - ローカル（WFS経由）: 使用するワークフロー（専用ワークフローの指定）等。既存の `requestPanelImageFromWorkflowStudio()`（WFS経由のT2I）を利用する。
  - Gemini: 画像モデル・2K指定（`imageSize`）等。既存の Nanobanana 生成（`requestNanobananaGenerate`）を利用する。
- **保存先: ComfyUIの output フォルダ直下の `cc_auto/`**。既存の `cc_nanobanana`（`py/config.py`の`OUTPUT_NANOBANANA_DIR`、保存API `save-nanobanana-image`、静的配信 `/ccc_nanobanana_output`）と同じ構成で追加する:
  - `py/config.py`: `OUTPUT_AUTO_DIR = Path(folder_paths.get_output_directory()) / "cc_auto"`
  - `py/ccc.py`: 保存API（例: `save-auto-image`、`_safe_path`でパストラバーサル対策）と静的配信（例: `/ccc_auto_output`）
  - **Pythonの変更なので、検証にはComfyUIの再起動が必要**。
- WFS経由の生成結果はblob URL（発行元ページ限りの一時参照）で返るため、保存前にデータURL化する（既存の `_urlToDataUrl` と同じ手法）。
- チャットからの呼び出しは**生成ボタン方式を主**とし、ツール呼び出し方式は対応モデル向けの追加とする（§8で確定）。ローカルLLMにはツール呼び出し非対応のモデルがあるため。

## 6. データ（新規作品として保存）

- **既存スクリプトタブとは連携しない**。Autoタブ用の**新規作品データ**を持つ（実装前確認点2・3・4は「連携なし・新規作品」で確定）。
- 保持する内容: お題、ページ数、ストーリー、脚本（ページ/コマ構造）、メモ、チャット履歴（作品データに含めて保存、§8で確定）。
- 保存方式の案: 既存スクリプト作品と同様に localStorage（作品一覧＋作業中データのオートセーブ）。
  既存のスクリプト作品リスト（`_SCRIPT_WORKS_KEY`）とは**別のキー**にして混在させない。
- 脚本の構造は、将来のレイアウト連携（機能2・3、および既存の流し込み/フキダシ生成）を見越し、参考アプリの項目を土台にする:

| 項目 | 内容 |
|---|---|
| ページ番号 / コマ番号 | 順序。ページ数の上限なし（参考アプリの1〜4固定は踏襲しない） |
| importance（High/Medium/Low） | 後続のコマサイズ按分（機能2）で使用 |
| action | コマの背景描写・演技指示（下描きガイド） |
| dialogue（複数・話者付き） | 参考アプリは1コマ1セリフだが、CCの既存構造（`{character, text, shape}`）に合わせて複数可にすると将来の連携が容易 |
| bubbleType | speech/thought/shout/narrator（既存のフキダシ形状 `shape` への対応表: speech→normal, thought→thought, shout→bomb, narrator→**caption**〔2026-09-22新設、直角・尻尾なし専用タイプ。詳細は§12〕。`auto-layout-core.js`の`bubbleTypeToBalloonShape()`で実装済み） |

- LLM出力のプロンプト生成・パースは DOM 非依存の純ロジック（`auto-comic-core.js`、または新規ファイル）に置く。

## 7. 実装ステップ（案・ストーリー・脚本タブ）

1. **タブ枠**: `templates/index.html` に `data-tab="auto"` を追加（Nanobananaタブの近く）。i18n（ja/en/zh）追加、`01-state.js`の`switchTab`に初期化。
   サブタブ（ストーリー・脚本 / レイアウト。レイアウトは空の枠のみ）と3ペインの骨組み。新規 `28-auto-tab.js`（`27-video-bridge.js`の次番号）。
2. **AIクライアント**（§4-4）: WFS `ai-tab.js`から必要部分を移植した新規ESMモジュール。Ollama/LM Studio/Lemonade/Unslothに対応。
3. **設定タブ**: BACKEND/CONNECTION/MODEL/GENERATION（WFS AI TOOL設定と同形）、Gemini（モデル選択）、画像生成欄。CC側（localStorage）に保存。
4. **Gemini経路**: `py/ccc.py` にテキスト生成エンドポイント（複数ターンのメッセージ配列・システムプロンプト対応）。
5. **左ペイン＋2段階作成**（§3-1）: お題・ページ数 →ストーリー生成 →（編集）→ 脚本生成。脚本のパースは二段構え（JSON抽出→行ベース復元）。
   失敗時に生出力を確認・再試行できるUI。「サンプル」ボタン。
6. **中央ペイン**: ストーリー/脚本/メモの表示・編集、オートセーブ、作品の保存・読込。
7. **Chatタブ**: 会話、Chatによるストーリー・脚本の編集、Auto向けツール（登録式）、スキル（必要なら）。
8. **画像生成**: 設定タブの画像生成欄、ラジオボタンでのエンジン選択、`cc_auto`への保存（Python側の追加を含む）。
9. **仕上げ**: ヘルプ（ja/en/zh）・README（3言語）・DEVLOG・PLAN_backlog.md の更新。Kaptureでの実機確認。

## 8. 実装前の確認事項（すべて確定・2026-09-21）

確認点はすべて回答済み。**実装は次の内容で開始できる状態**。

| # | 項目 | 確定内容 |
|---|---|---|
| 1 | LLMクライアント | WFS `ai-tab.js` から必要部分をCCへ移植（§4-4） |
| 2 | 作成フロー | 2段階（ストーリー → 編集 → 脚本）（§3-1） |
| 3 | Chat/TOOLS | Chatでストーリー・脚本を編集。Auto向けツールは登録式。翻訳・VLMはレイアウトタブ作業後（§5-2） |
| 4 | 画像生成 | ローカル(WFS経由)/Geminiのラジオボタン選択、設定は設定タブ「画像生成」欄、保存先`output/cc_auto/`（§5-3） |
| 5 | Chatからの反映方式 | 返答に付く**「ストーリーに反映」「脚本に反映」ボタン**で置換/追記（Undo付き）。ツール呼び出しによる直接編集は後から追加可能 |
| 6 | 脚本タブの形式 | ページ/コマ単位の**構造化された表・カード**。Chatとのやり取りは内部でJSON⇔表示を変換 |
| 7 | 画像生成の呼び出し方式 | チャット上の**生成ボタン**を主（LLMはプロンプト作成のみ）。ツール呼び出し（`generate_image`）は対応モデルでの追加 |
| 8 | I2I | 初版に含めない（T2Iのみ）。I2Iは後続 |
| 9 | ストーリー空のときの脚本生成 | 「ストーリーから脚本を作成」ボタンを**無効化**（2段階の徹底） |
| 10 | チャット履歴・メモ・作品管理 | チャット履歴は作品データに含めて保存、メモはプレーンテキスト、作品の新規/保存/読込/削除は既存スクリプトタブと同じ操作感 |

## 9. リスク・注意点

- **LLM出力の不安定さ**: 小型ローカルモデルではJSONが崩れやすい。パース失敗時のフォールバックと再試行UIが必須。
- **長文のJSON出力**: 全ページ分は長いため、Max tokens・Thinking modeの設定が結果に直結する（移植するLLMクライアントでは設定を必ず渡す）。
- **構文検証**: ヘルプ等の長い文字列リテラルを編集した後は、`node --check`だけでなく`vm.SourceTextModule`か実機で確認する（過去に`node --check`がESM構文エラーを見逃した事例あり）。
- **WFS iframe の別コンテキスト問題**: 画像生成でWFS経由（ローカル）を使う場合、対象は「CC画面内に埋め込まれたWFSタブ」であり、別タブの `/wfm` とはJS状態を共有しない。ヘルプにも明記する。
- **WFSのファイル変更**: LLMクライアントをCCへ移植する方針のためWFS側の変更は原則不要。WFSを変更した場合のみ、WFS側は通常コピーのため動作確認前に `custom_nodes\comfyui-workflow-studio\` へ手動コピーが必要（CC側は自動反映）。
- **リリース作業**: バージョンアップ・タグ・リリースは明示指示があるまで行わない。

## 10. 参考

- 参考アプリ: `now_work/manga_panel/`（`src/App.tsx`, `src/presets.ts`, `server.ts`, `DEVLOG.md`）
- CC既存: `static/js/main/21-script-tab.js`（作品のlocalStorage保存の方式）、`21a-script-manga.js`（脚本データ構造）、`26-auto-comic-bridge.js`（流し込み/フキダシ/一括生成）、
  `14-integrations.js`（WFS橋渡し、`requestPanelImageFromWorkflowStudio`）、`static/js/auto-comic-core.js`（純ロジック）、`static/js/nanobanana.js` と `py/ccc.py`（Gemini）
- WFS: `ComfyUI-Workflow-Studio/static/js/ai-tab.js`（設定・`callLLM`/`callChat`・Chat・VLM・スキル）、`templates/index.html`（AIタブのHTML）、`static/js/app.js`（サブタブ）

## 11. 後続検討: 軽量な「判定」レイヤー（TypeSafe Jev / ローカルlogprobs方式）

**状態: 検討のみ（2026-09-21）。実装は未着手・着手時期未定。** レイアウトサブタブの計画時に再検討する。

### 11-1. 考え方
- [TypeSafe Jev](https://typesafe.ai/)は文章生成ではなく、自然言語から**型付きの判定（選択・yes/noの確率・段階スコア）**を返すモデル。ストーリー・脚本の生成は現行LLMに任せ、判定だけを担当させる補助レイヤーとして使う。
- 同種のローカル方式: [Qwen3-VL-4BでJev風のローカルAPI実装](https://qiita.com/moritalous/items/41c9402a5dd9d80fc7a9)（llama.cpp）。**`max_tokens:1` + `logprobs`（上位候補）**で1トークン目の確率分布を取り、選択肢の記号だけに絞って正規化する。約110ms。画像入力にも対応できる（Jev本家はテキストのみ）。

### 11-2. Ollamaでの実測結果（2026-09-21、Ollama 0.34.2、RTX 4070 12GB）
- `logprobs` / `top_logprobs` は `/v1/chat/completions` と `/api/chat` の**両方で返る**。検索で見つかった issue #16117（OpenAI互換側でlogprobsが無視される）は0.34.2では該当しなかった（他バージョンは未確認）。
- 検証モデル: `gemma4:e4b`、`qwen3.5:9b`（`reasoning_effort:"none"` / native は `think:false` を付与）。
- フキダシ種別4択（A通常/B心の声/C叫び/Dナレーション）とyes/noの判定は、両モデルとも妥当な結果（最頻候補の確率0.92〜1.0）。ロード後は1回140〜220ms、初回はモデルロードで6〜9秒。
- 注意点:
  - 上位候補に選択肢外トークン（`**`、`答え`、`N`、`ナ`等）が混ざる → **選択肢の記号のみ拾って正規化**する。yes/noは`yes`/`Yes`/`はい`を合算。
  - 選択肢は**1トークンで出せる記号**（A〜D、yes/no、数字）に限る。
  - 確率が極端に偏る（曖昧なはずの入力でも0.93〜1.0）。Jevのような較正は無い前提で、閾値は実データで決める。
  - 試行は6件程度で精度評価ではない。`qwen3.5:9b`で連続テスト中に接続切断が1回あった（同入力の再実行では再現せず、原因未調査）。
  - VRAMはComfyUIの画像生成と共有。ストーリー用LLMと**同じモデルを判定にも使う**とモデル入れ替えを避けられる。
- **未検証**: LM Studio/Lemonade/Unslothのlogprobs対応、画像入力での判定（Qwen3.5系はハイブリッドメモリ構造でキャッシュが効かない旨が記事にある）、Geminiのlogprobs、TypeSafe API自体の仕様・料金・日本語精度（docsは概念ページのみ確認）。

### 11-3. 設計方針（着手する場合）
- `auto-ai-client.js` に判定関数（`judge()`等）を追加し、バックエンドを切替可能にする。
  - **ローカル（既定）**: 既存のLLM設定（Ollama等）を流用。APIキー中継不要・無料。
  - **TypeSafe Jev（任意）**: 較正精度が必要なときのみ。APIキーはNanobanana同様にpy側保持し `py/ccc.py` に中継エンドポイントを新設。外部呼び出しの実行前にユーザーへ確認（Geminiと同じ扱い）。
- 判定の分解: 独立した問いを1つずつ立て、素の確率をデータとして保持する。重みや閾値はコード/UI側で変え、変更時に再推論しない。

### 11-4. 活用案（優先度順）
1. **コマ重要度の連続スコア化**（レイアウトサブタブ向け）: 盛り上がり度・情報量・感情の強さ等を各コマにScore判定し、重み付き合成でコマサイズ按分に使う。現状のHigh/Medium/Lowの3段階より滑らか。
2. **`bubbleType`自動判定**: セリフを speech/thought/shout/narrator/whisper に分類し、既存フキダシ`shape`への対応表と組み合わせる。
3. **Chatの意図ルーティング＋引数抽出**: ストーリー編集/脚本編集/セリフ推敲/画像生成/雑談の分類と、対象ページ・コマ・セリフの特定。ツール呼び出しが弱い小型LLMでも編集対象を決められる（§8-5「ツール呼び出しによる直接編集は後から」に効く）。
4. **脚本の整合性チェック**: コマ単位でNoulを並列に問う（キャラ設定との矛盾、場所・時間の飛び、セリフの長さ）。不合格のコマだけChatのLLMへ再生成させる。
5. **セリフ案の複数生成→選択**: LLMに複数案を出させ、口調適合・自然さをScoreしてベストを選ぶ（セリフ推敲ツールの品質向上）。
6. **改ページ位置の最適化**: 各コマの「ページ末の引き」適性をScoreし、ページ数制約のもとで総スコア最大の区切りを選ぶ。
7. **演出タグ判定**: ショットタイプ（アップ/バストアップ/全身/引き）と感情を判定し、下描きガイド・画像生成プロンプト・コマ形状・VRMポーズ/表情プリセット選択に使う。
8. **緊張度カーブの可視化**: 案1のスコアを折れ線で脚本タブに表示（テンポメーター）。
- 最初の試作候補: 案1・案2・案4（脚本データ構造§6にそのまま乗る）。案3はChat作り込みと併せて。
- 画像入力の判定（生成画像が脚本に合っているか等）はローカルVLM方式のみの可能性。要検証。

## 12. 実装まとめ: 「オートレイアウト」サブタブ（第2弾、2026-09-22実装・実機確認済み）

第1弾完成後、脚本からのコマ割り自動生成・既存レイアウトタブへの転送を実装した。ユーザーのモックアップ（左ペイン: ページ設定/テンプレート/詳細設定のタブ切替、選択・分割ツール、コマ数表示、右ペイン: プロパティ〔フキダシ/画像〕）に基づき、当初案（§2表の機能2・3）よりも作り込んだ「フルセット」構成で実装した。サブタブ名は既存の「レイアウト」タブと混同しないよう**「オートレイアウト」**に確定（ユーザー指定）。

### 12-1. 自動配置アルゴリズム（`static/js/auto-layout-core.js`, DOM非依存）

参考アプリ`manga_panel`の2機能（`App.tsx`の重要度按分=縦1列のみ、`presets.ts`の再帰分割=順序非保存の貪欲二分割）はどちらも単独では脚本の読み順を保持できないため、**読み順を保ったまま重要度で面積按分する再帰二分割**に統合した独自設計を採用（`layoutPanels()`）。重み`{High:3, Medium:2, Low:1}`、5スタイル（balanced/asymmetric/diagonal/vertical-strip/horizontal-strip）、読み順（右から左=既定／左から右）に対応。quadは4頂点で表現し、diagonalスタイルは非矩形（斜め継ぎ目）になる。軸判定（縦線/横線どちらで割るか）は`pageAspect`（実際のページ幅/高さ比）を反映しないと常に正方形近似で誤判定するため、`contentAreaFromMargins()`経由でページの実アスペクト比を渡す設計にした。

### 12-2. ページ設定（サイズプリセット・外側余白）

- **サイズプリセット**: ページタブの「新規作成」ダイアログ（`11a-work-manager.js`の`_workSizePresetList()`標準6種・`_workSizePresets`カスタム）をそのままimportして共有。カスタムプリセットの保存先（localStorage `work_size_presets`）が同じため、オートレイアウトで保存したプリセットはページタブの新規作成ダイアログでも即座に使え、双方向共有される（実機確認済み）。
- **外側余白（上下左右）**: `contentAreaFromMargins(opts)`で「余白を除いたコマ配置領域」の幅・高さ・オフセットを算出し、`layoutPanels()`の軸判定・`quadToPagePoints()`の両方に一貫して渡す。「均等」チェックボックスで4値を連動編集可能（work.layoutには保存せず、4値一致時に自動でON判定するUI状態のみ）。
- **既定コマ枠線幅**: 従来ハードコードだった`strokeWidth`（既定60）を詳細設定タブで設定可能にした。
- **ページ設定/詳細設定の保存を分離**: 「生成（全ページ）」ボタン（コマ割りを再計算するため確認ダイアログあり）はページ設定タブのみに配置。既定フォント/既定サイズ/既定コマ枠線幅（生成には使わず転送時にのみ使う値）は詳細設定タブ専用の「保存」ボタン（確認ダイアログなし・再生成なし）で即時保存する。ユーザーからの「生成ボタンを押さないとフォント設定が保存されないと誤解する／生成ボタンでコマ割りが変わると誤解する」という混乱防止の指摘を受けて分離した（`onSavePageSettings()` / `onSaveDetailSettings()`）。

### 12-3. UI（`static/js/main/28b-auto-layout-tab.js`、DOM操作担当）

3ペイン構成。中央ペインは選択/分割ツール＋ズーム＋ページ送り付きのSVGプレビュー（`renderCanvas()`）。「分割」ツールは選択中コマを1回だけ二分割し、対応する脚本コマも複製して1対1対応を保つ。右ペインのプロパティパネルはフキダシ（追加・編集・削除）と画像（既存 `28a-auto-image.js` の `generateAutoImage()` で生成→`panel.imageUrl`に保存、または`auto.work.images[]`のギャラリーから選択）を扱う。左ペインの「テンプレート」タブでは、既存レイアウトタブの保存済みテンプレートをコマ数一致で選び、算出レイアウトの代わりに使える。

作品データ（`auto.work`）は`static/js/main/28-auto-tab.js`が所有し、本ファイルは`getAutoWork`/`saveAutoWork`経由で読み書きする循環import構成（`PLAN_auto_tab.md`既出の設計方針どおり、循環先シンボルの参照が関数内部に閉じていれば安全というこのコードベース全体の既存パターンを踏襲）。

### 12-4. レイアウトタブへの転送

各スクリプトページについて、プレビューで確定したquads（手動分割済みのものを含む）から`quadsToPageLayout()`でページレコード（panels[]・svgContent）を組み立て、`dbPut('pages', ...)`→`state.activePage`に設定→`renderLayoutTab()`。フキダシ生成は`mapScriptPageToPanels()`（既存`auto-comic-core.js`）でパネルとセリフを対応付け、半自動マンガ作成（`26-auto-comic-bridge.js`の`_handleAutoBalloonGenerateClick`）と同じ関数群（`createBalloonAtPosition`/`applyBubbleTextToShape`）を再利用。画像は対応するスクリプトコマに`imageUrl`があれば`insertImage()`で挿入。転送先ページは`_pageMgrGroups.createGroup()`+`_workMeta.set()`+`_pageMgrGroups.assign()`+`_workSetActive()`で「作品」としてページ管理タブの作品一覧にも登録する（登録しないとページ自体は作成・編集できるが作品一覧に出ず見失いやすい）。

### 12-5. 実機で見つけて修正したバグ（すべて2026-09-22発覚・修正・実機確認済み）

1. **転送後ページの背景が透過**: 実際のテンプレートSVGは`<polygon id="panel_0">`より前に`<rect fill="#ffffff">`の背景矩形を持つが、生成SVGにはこれが無かった。`quadsToPageLayout()`に同じ背景rectを追加して解決。
2. **手動「分割」ツールで分割したコマが転送後は違う配置になる（コマ数は合っている）**: 転送処理が非テンプレートページに対し常に`layoutPanels()`を再実行しており、プレビューでの手動分割結果を無視して脚本panels[]から新規に計算し直していた。`auto-layout-core.js`を「既存quads→panels/svgContent変換のみ」の`quadsToPageLayout()`と「新規生成」の`buildPageLayout()`に分離し、転送処理は必ず`st.quads`（プレビューの確定形状）を直接渡すよう修正。**教訓**: プレビュー用の確定済みジオメトリを持っている場合、転送・保存等の最終処理では必ずそれを直接使い、元データから再計算しない。
3. **外側余白の入力欄が狭すぎて数値が読めない→誤入力でコマが全く表示されなくなる**: 1文字ラベル（上/下/左/右）に既存の長いラベル用固定幅クラスを流用したため入力欄がほぼ潰れ、狭い欄への誤入力で余白が`1011111010`という異常値になり、コマ配置領域がページ外（viewBox外）へ押し出されて何も描画されなくなった。専用の狭いラベル幅クラスで表示を修正し、`contentAreaFromMargins()`に「余白合計がページ幅/高さの90%を超えたら比例縮小してクランプ」する下限側の最終防御を追加、さらに読み込み時（`normalizeLayoutSettings()`）・保存時（`onSavePageSettings()`）の両方でページ幅/高さ自体を上限に余白をクランプ（異常値をそもそも保存させない）。**教訓**: 数値入力UIを狭くする変更をした後は、実際にその狭さで誤入力しても壊れないか（境界値クランプ）も合わせて点検すること。

### 12-6. フキダシ「キャプション」種別の追加（同日、関連作業）

オートレイアウトのnarrator用フキダシとして使うため、既存の角丸矩形（rect、常に角丸半径調整可・尻尾システム付き）とは別に、常に直角・尻尾なし専用の新種別「caption」を追加した。既存のH2フキダシ共通システム（`09b-balloon-shapes.js`の`_updateH2ShapePath`等）にcaption分岐を追加し、パラメータパネルは表示させない設計（尻尾関連スライダーが不要なため）。色・枠線変更・テキスト内包は既存フィキダシと同様に使える（`09a-balloon-init.js`の`_isH2Type`、`09f-bubble-text.js`の`_isH2BalloonType`に追加）。延長フキダシ機能は対象外（尻尾を持たないため不要）。レイヤーパネルの専用アイコン（▬）追加。`auto-layout-core.js`のnarrator→captionマッピング済み。i18n（ja/en/zh、`layout.balloonCaption`）追加。

### 12-7. 残作業・未実機検証項目

- **未実機検証**: テンプレート対応付けタブ（既存テンプレートをコマ数一致で使う経路）、画像生成/ギャラリーピッカー（LLM・画像生成の実行を伴うため）、asymmetric/horizontal-strip スタイル、左から右の読み順。balanced/diagonal/vertical-strip・rtl・フキダシ編集・分割ツール・転送・作品登録・キャプションフキダシ・§12-8の追加改善8件は実機確認済み。
- **ヘルプ（ja/en/zh）・README（3言語）は2026-09-24〜25にすべて反映済み**（§12-8・§12-9参照）。
- バージョン更新・リリースは未実施（明示指示があるまで行わない）。
- 半自動マンガ作成（既存スクリプトタブ、`26-auto-comic-bridge.js`）とオートレイアウトは実装が並行（似た転送ロジックを別々に持つ）。将来的に共通化を検討する余地はあるが、今回は既存コードへの影響を避けるため独立実装とした。

### 12-8. 追加改善8件（2026-09-24、ユーザーフィードバックにより実施・すべて実機確認済み）

第2弾の実機確認後、ユーザーが実際にオートレイアウトサブタブを使う中で挙がった改善要望に対応した。

1. **フォントタブ連携**: フォントタブ右パネルの「選択テキストに適用」ボタンの下に「オートレイアウト」欄を新設し、「既定フォントに設定」ボタンでオートレイアウトの詳細設定タブと同じ`work.layout.fontFamily`を直接設定できるようにした（確認ダイアログなしで即座に自動保存）。`28-auto-tab.js`の`getAutoWork()`を、`auto.work`が未初期化（Autoタブを一度も開いていない）でも呼べるようnullセーフ化した（`static/js/main/19-font-manager.js`, `20-font-presets.js`, `28-auto-tab.js`）。フォント名への拡張子表示は、実現にはLocal Font Access APIのバイナリ読み込みが必要と判明したためユーザー判断で見送り。
2. **コマ選択プロパティのUI調整**: 右ペインのフキダシ「変更」「追加」ボタンを2行目へ移動（セリフ選択セレクトを1行目に単独化）。画像プロンプト入力欄を既定7行表示・`resize:vertical`でドラッグによるさらなる拡張に対応（`templates/index.html`）。
3. **転送時のz-orderバグ修正**: `insertBalloonsAndImages()`（`28b-auto-layout-tab.js`）が「フキダシ作成→画像挿入」の順で処理していたため、`insertImage()`が常にコマのg要素内の最後（＝最前面）に`<image>`を追加する仕様と噛み合わず、画像がフキダシを覆い隠すことがあった。処理順序を「画像挿入→フキダシ作成」に入れ替え、画像挿入後（`renderLayoutTab()`でDOM再構築後）に最新のパネルレイヤーSVGを取得し直してからフキダシを追加するよう修正。フキダシは常に画像より前面になる（フキダシ同士の重なりは従来どおり許容）。
4. **コマ間余白と外側余白の独立化（2段階で修正）**: `auto-layout-core.js`の`quadToPagePoints()`（コマ間余白＝gapPxの実現方法）を2段階で修正した。
   - 第1段階: 従来は全頂点を四角形の重心方向へ一律にinsetしていたため、ページ外周（＝外側余白の境界）に接する頂点も内側へ縮み、外側余白とコマ間余白の半分が合算されて見えていた。頂点の座標成分が外周（quadの0/100%）と一致する場合はその成分を固定するよう変更。
   - 第2段階（ユーザー指摘で発覚した副作用の修正）: 第1段階の頂点単位オフセットは、非対称・斜め継ぎ目スタイルで共有辺を挟む両側のコマの重心位置が異なる場合、区切り線が斜めに歪む副作用があった。**辺単位の多角形オフセット**（各辺をその内向き法線方向へgapPx/2だけ平行移動し、隣接辺同士の交点を新頂点にする）方式に書き換え、共有辺は常に元の辺と平行になるよう修正（Node.jsで斜め共有辺でも法線方向の距離が正確にgapPxになることを数値検証、Kaptureで斜め継ぎ目・非対称スタイルの実機確認済み）。
5. **生成のランダム性**: 「生成（全ページ）」ボタン（`generateAllPages()`）を押すたびに新しい疑似乱数シード基点（`alo.seedBase`、`Math.random().toString(36)`）を採番し、非対称・斜め継ぎ目スタイルの配置が毎回変化するようにした。シード自体は作品データ（`work.layout`）には保存しない。ページ送り等での再生成（`ensurePageLayoutFresh()`等）は同一セッション内の`alo.seedBase`を使い回し、生成ボタンを押していない場面で無意味に見た目が変わらないようにしている。
6. **ツールボタンのUI改善**: 選択/分割ツールのアクティブ状態のスタイルを、ボーダー強調のみから他の同種トグルボタン群と共通の青背景（`background-color: var(--primary-color)`）に変更（`auto-tab.css`）。「分割」ツール1つを「分割(縦)」（縦線で分割＝左右に分かれる）・「分割(横)」（横線で分割＝上下に分かれる）の2ボタンに分離し、`splitQuadManual(quad, { axis })`へ明示的に軸を渡すようにした（従来はコマの幅・高さ比較による自動判定のみ）。
7. **作成言語のUI設定言語対応**: 「作成」（ストーリー生成）・「ストーリーから脚本を作成」時、CCのUI設定言語（`i18n.js`の`getLang()`、ja/en/zh）に応じてLLMへ言語指示を追加するよう`auto-story-core.js`の`storySystemPrompt()`/`scriptSystemPrompt()`を書き換えた。脚本のJSON構造・キー名・`importance`/`bubbleType`の値は言語に関わらず維持し、`"action"`・`"character"`・`"text"`の中身のみ設定言語で書くよう指示する。実際にその言語で書かれるかは使用モデルの能力次第（結果は保証しない）。日本語（既定）の出力は変更前と完全一致することを確認済み。
8. **ヘルプ反映**: `22-help-tab.js`のAutoタブヘルプで「レイアウトは今後追加予定」のまま放置されていた記述を更新し、オートレイアウトサブタブ全体（ページ設定・テンプレート・詳細設定・中央ペイン・右ペイン・レイアウトタブへの転送）の説明を新規追加。フォントタブヘルプにも「オートレイアウト連携（既定フォントに設定ボタン）」を追加。

**How to apply**: 上記5（辺単位の多角形オフセット）は`quadToPagePoints()`の実装が丸ごと変わっているため、今後このファイルを触る際は頂点単位の重心方向inset（旧実装）に戻さないよう注意。README（3言語）は2026-09-24中に反映済み（DEVLOG参照）。

### 12-9. 作成言語対応のバグ修正（2026-09-25、§12-8の7への追加修正）

§12-8の7（作成言語のUI設定言語対応）実装後、実機で2件の問題が発覚し修正した。

1. **見出しの言語混在バグ**: 英語UIでストーリーを作成すると「# 【Title】」のように日本語の【】記号と英語が混在する出力になった。`storySystemPrompt(lang)`が見出し（【タイトル】等）を日本語固定のまま渡していたため、モデルが見出し記号自体まで中途半端に訳そうとして崩れていたと考えられる。見出し記号自体もUI設定言語に応じて切り替える`STORY_HEADINGS`（ja: 【タイトル】等／en: `[Title]`等／zh: 【标题】等）を新設し、システムプロンプトに「この記号のまま使う（翻訳しない）」旨を明記して解決した。**教訓**: LLMへの指示で「本文だけ他言語にして、決まった記号は元言語のまま使ってほしい」という部分的な言語指定は、モデルが記号自体まで律儀に（あるいは中途半端に）訳そうとして崩れることがある。テンプレート的な記号・見出しも指示する言語に合わせて用意する方が安全。
2. **サンプルが日本語固定**: 「サンプル」ボタンの`SAMPLE_THEME`/`SAMPLE_STORY`/`SAMPLE_SCRIPT`（`auto-story-core.js`）が日本語固定の定数だったため、UI設定言語を切り替えても常に日本語が読み込まれていた。同じ内容（柴犬の宇宙飛行士ポチの話）を英語・中国語にも翻訳し`_BY_LANG`のオブジェクトに変更、`getSampleTheme(lang)`/`getSampleStory(lang)`/`cloneSampleScript(lang)`という関数APIに変更した（呼び出し元`28-auto-tab.js`の`onSample()`で`getLang()`を渡す）。

実機検証（Kapture、UI言語をen→zh→jaの順に切替）で、それぞれのサンプルのお題・見出し・本文が一貫して対応言語になることを確認済み。ヘルプ・README（3言語）・DEVLOG（2026-09-25エントリ）に反映済み。

### 12-10. 脚本パースの頑健化・レイアウトGenerateモーダルとのAuto連携（2026-09-25）

§12-9とは別に、ユーザーからの2件の要望・不具合報告に対応した。

1. **脚本パースの頑健化（LLM出力の閉じ括弧不足を自動修復）**: ユーザーが「ストーリーから脚本を作成」時に「LLMの出力から脚本を読み取れませんでした」に遭遇し、提示された実際の生出力を調査した結果、LLM出力がpage2のパネル配列の途中で打ち切られており（`pages`配列とルートオブジェクトの閉じ括弧`]}`が2つ欠落、おそらくMax tokens到達）、末尾に`]}`を1つ補うだけで完全な有効JSONに復元できることを確認した。既存の「行ベース復元」フォールバックは改行区切りを前提にしていたため、改行のない1行JSON出力（今回のケース）では機能せず、結果的に読み取り失敗になっていた。`repairTruncatedJson()`（`auto-story-core.js`）を新設し、文字列リテラルの中身を無視しつつ開き括弧をスタックで数え、不足分の閉じ括弧（と閉じられていない文字列があればその閉じクォート）を末尾に補ってから再度JSON.parseを試みるステップを、通常のJSON.parse失敗後・行ベース復元の前に`parseScriptResponse()`へ追加した（新しい`method: 'json-repaired'`。呼び出し元`28-auto-tab.js`は`parsed.method === 'lines'`だった判定を`parsed.method !== 'json'`に変更し、'json-repaired'でも生出力確認を促す表示にした）。ユーザー提示の実際の壊れたJSONで全2ページ分すべてが正しく復元されることをNode.jsで検証済み。
2. **レイアウトタブ「Generate」モーダルとAutoの連携（Autoから取得）**: 当初ユーザーは「レイアウトのGenerate機能でストーリー・脚本を連携させバッチ生成を可能にしたい」と要望し、最初はスクリプトタブの半自動マンガ作成（`26-auto-comic-bridge.js`の「画像を一括生成」モーダル）の話だと誤解したが、実際はレイアウトタブの「Generate」ボタンから開く`openLayoutI2IModal()`（`15-pixifx-bridge.js`）のことだった（ユーザーがモーダルのスクリーンショットを提示して訂正）。**教訓**: 「Generate」という名前のボタン・モーダルは複数箇所に存在する（スクリプトタブの半自動マンガ作成、レイアウトタブのI2I/T2Iモーダル）ため、名前だけで対象を決め打ちせず、曖昧なら早めに実物のスクリーンショット等で確認したほうがよい。対応付けの設計については「詳細編集（レイアウトタブへ転送）とは切り離し、現在Autoタブで作業中の作品・ページ指定という形」とユーザーから明確な指示を得て実装した。モーダル上部に「Auto」ラベル・脚本ページ選択セレクト・「Autoから取得」ボタンを新設し、押すと現在Autoタブで作業中の作品（`getAutoWork()`）のストーリー全文を「全体」タブのPositiveへ、選択した脚本ページの各コマの`action`を対応する「コマN」タブのPositiveへ、それぞれ反映する（上書き）。コマの対応付けは`state.activePage.panels`と脚本`page.panels`を配列の順序のみで対応させる（転送済みページと元のAuto脚本ページの正式な対応関係は保存されていないため）。コマ数が一致しない場合は一致する範囲のみ反映しステータスに表示する。

実機検証（Kapture）: レイアウトタブのGenerateモーダルで「Autoから取得」を押し、「全体」タブへストーリー全文が、「コマ1」タブへ対応するactionが正しく反映されることを確認済み。ヘルプ・README（3言語）・DEVLOG（2026-09-25エントリ）に反映済み。

### 12-11. オートレイアウト × ページタブのテンプレート連携・詳細設定の既定フォントサイズ変更（2026-09-25、§12-10とは別セッション）

「オートレイアウトで作ったコマ割りをテンプレートとして保存したい」「テンプレート数が増えるとオートレイアウト側の狭い左ペインに一覧が収まらない」という要望・懸念を受け、テンプレートの一覧閲覧・管理をページタブ側に一本化する設計に変更した。

**設計変更（一覧→単一送信スロット方式）**: 従来、オートレイアウトの「テンプレート」タブ（`28b-auto-layout-tab.js`）はページタブと同じ保存済みテンプレート一覧を独自に表示していた（§12参照）が、テンプレート数が増えると狭い左ペインでの一覧表示が破綻する懸念があったため、一覧の閲覧・作成・管理はページタブ側（`06c-template-wizard.js`）に一本化し、オートレイアウト側は「送信された1件だけ」を表示する方式に変更した。

- **ページタブ側**: 各テンプレートカードには何も追加せず、上部（「ページとして挿入」の右隣）に**「オートレイアウトへ送信」**ボタン（`#template-send-auto-btn`）を新設。選択中の1件に対して動作し、押すと`_tmplAutoStage`（`06b-template-manager.js`、localStorageキー`auto_layout_staged_template`で単一のテンプレート名を保持する小さなストア）にセットする。送信は常に1件のみで、別のテンプレートを送信し直すと置き換わる。当初は各カードに個別の送信/解除トグルボタンを付ける案で実装したが、ユーザーから「テンプレートごとではなく上部に、選択テンプレートに対して動作させたい。解除ボタンは無くてよい（別のテンプレートを送信するか、オートレイアウト側の選択解除で足りる）」との指示を受けて上記の形に修正した。テンプレートの削除・リネーム時に`_tmplAutoStage`が古いテンプレート名を指したままにならないよう追従処理も追加（削除時はクリア、リネーム時は新名に追従）。
- **オートレイアウト側**: `renderTemplateList()`を全面書き換えし、`_tmplAutoStage.get()`で得た1件のみを`#alo-template-staged`に表示（従来の`#alo-template-list`は廃止）。クリックすると`onTemplatePick()`でそのページへ割り当てる動作は従来どおり。「選択解除」ボタン（旧「テンプレート指定を解除（自動生成に戻す）」）は、そのページのテンプレート指定を解除するのに加えて`_tmplAutoStage.clear()`も呼ぶようにし、送信状態自体もあわせて解除する（送信専用の解除ボタンは持たない方針のため）。
- **「テンプレートとして保存」ボタン新設**: オートレイアウトに現在表示中のページ（`currentPageState()`＝`alo.pages[alo.pageIndex]`。脚本のこのページのコマ割りで、まだフキダシ・画像は実体化されていない段階）を、ページタブの新規テンプレートとして保存できるボタンを追加した（`onSavePageAsTemplate()`）。`st.quads`（自動生成/手動分割済みのコマ形状）から`auto-layout-core.js`の`quadsToPageLayout(quads, work().layout)`で`panels[]`/`svgContent`を組み立て、`06b-template-manager.js`に新設した`savePageAsTemplate(pageRecord, name)`でテンプレートとして`dbPut`する。**初版実装では誤って`state.activePage`（レイアウトタブ側のアクティブページ）から保存していたが、ユーザーから「保存対象はレイアウトタブのページではなく、オートレイアウトで表示中のページ」と指摘があり修正した**（テンプレート自体が`st.template`の場合はそのテンプレートのpanels/svgContentをそのまま複製）。
- **ボタン名の短縮**: 「表示中のページをテンプレートとして保存」→「テンプレートとして保存」、「テンプレート指定を解除（自動生成に戻す）」→「選択解除」に短縮（下に説明文があるため簡略化、狭い左ペインでのボタン見切れ改善も兼ねる。tooltipに詳細説明を追加）。

**実装前の確認**: 着手前に、レイヤーパネルに現れる各オブジェクト種別（フキダシ/画像/テキスト/描画図形/グループ/マスクレイヤー）で表示/非表示の仕組みが同じかどうか調査した経緯があり（後述のAlt+クリック一括切替機能、レイアウトタブ側の関連作業）、同様にこのテンプレート連携についても設計を確定する前にユーザーへAskUserQuestionで確認してから実装した。

**実機検証（Kapture）**: ページタブでテンプレートを選択→「オートレイアウトへ送信」→オートレイアウト側の「テンプレート」タブに1件だけ表示されることを確認。同テンプレートを別のテンプレートで送信し直すと置き換わること、「選択解除」で送信状態自体も解除され空表示に戻ることを確認。オートレイアウトで非対称スタイルの4コマページを生成した状態で「テンプレートとして保存」→ページタブのテンプレート一覧に同じ非対称コマ割りのテンプレートが追加されることを確認（プレビュー画像で形状が一致することも確認）。検証用に作成したテンプレート・ページはいずれも削除済み。

**詳細設定の既定フォントサイズ変更**: `blankLayoutSettings()`（`28-auto-tab.js`）の既定`fontSizePt`を28→80に変更した（レイアウトタブの既定サイズに合わせたいというユーザー指定）。新規作成するAuto作品にのみ適用され、既存の保存済み作品の`work.layout.fontSizePt`は変わらない（ユーザーからは「今の作品はそのままでよい」と確認済み）。

**ヘルプ反映**: Autoタブ「テンプレート（左ペイン）」の説明を新しい単一送信スロット方式に合わせて全面的に書き直し、ページタブ「テンプレート」の使い方に「オートレイアウトへ送信」ボタンの説明を追加（日英中3言語）。

**How to apply**: 今後オートレイアウトのテンプレート機能に触れる際は、一覧UIは復活させず「送信された1件のみ表示」の設計を維持すること（テンプレート数が増えても破綻しないための意図的な制約）。「オートレイアウトで表示中のページ」と「レイアウトタブのアクティブページ（state.activePage）」は全く別物である点に注意（前者はまだフキダシ・画像が実体化されていないコマ割りのみのデータ）。README（3言語）・DEVLOG（2026-09-25エントリ）に反映済み。バージョン更新・リリースは未実施。
