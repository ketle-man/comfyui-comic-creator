# 作業計画: レイアウト（ページ）の外部編集可能フォーマット対応

作成日: 2026-09-07
ステータス: **フェーズ1(出力タブへのSVGエクスポート追加) 完了・ユーザーによる外部ソフト実地検証済み**。フェーズ2(編集後SVGの再インポート)は**ユーザー判断により見送り**（2026-09-07、「インポートに関しては結構です」）。ヘルプタブ・README（日英中3言語）への互換性情報の反映も2026-09-07に完了。詳細はDEVLOG.md「2026-09-07」参照。

## 背景・目的

Imageタブに実装したPSD(Photoshop)エクスポート（[[comic-creator-workflow]]参照）に続き、レイアウトタブ（ページ全体のコマ割り編集）の内容も外部ソフトで編集できるようにしたい、という要望を受けて調査した。

### 調査1: レイアウトをPSDでどこまで再現できるか

psd-tools（Pythonの書き込み用PSDライブラリ）の実際の書き込みAPIを確認した結果、**新規作成できるレイヤー種別は実質「PixelLayer(ラスター画像)」と「Group(フォルダ)」の2つのみ**と判明した。

| レイヤー種別 | 新規作成用コンストラクタ |
|---|---|
| PixelLayer(ラスター) | `frompil()` あり |
| Group(フォルダ) | `new()` / `PSDImage.create_group()` あり |
| TypeLayer(テキスト) | ❌ なし(読み込み専用) |
| ShapeLayer(ベクター図形) | ❌ なし |
| SmartObjectLayer | ❌ なし |
| レイヤー効果(ドロップシャドウ・光彩等) | ❌ 書き込みAPIなし |
| グラデーション塗りつぶしレイヤー | ❌ なし |

opacity・blendMode・visible・clipping_layer(下のレイヤーへのクリッピング)は設定可能。つまり**PSDでは「Photoshopで再編集可能な生きたテキスト/ベクター図形/レイヤー効果」としての出力は原理的に不可能**で、最終的にはすべて見た目をラスタライズした画像＋グループ階層に落とし込むしかない。

さらにレイアウトタブは、Imageタブの`LayerManager`のような明示的なレイヤー配列を持たず、複数のSVG文字列(`svgContent`+`panels[].panelSvgContent`+`overlaySvgContent`)を`buildMergedSvg()`で動的合成した結果としてしか完成形が存在しない（コマは多角形clipPath、フキダシは4パス+テキストの複合グループ、マスクは`<mask>`+ラスター画像という構造）。PSD化するには、SVG DOM→階層的なエクスポート対象への変換ロジックを新規に書く必要があり、Imageタブ版より実装コストが大きい。

**実現レベル案（PSD化する場合）:**

| レベル | 内容 | 技術難度 | 実用性 |
|---|---|---|---|
| L0 | ページ全体を1枚のPixelLayerに完全フラット化 | 低 | 低（PNG保存と大差なし） |
| L1 | コマごとにGroup、コマ内は1枚のラスターに統合。コマ形状はラスターのレイヤーマスクで再現 | 中 | コマ単位で移動・非表示・不透明度調整が可能 |
| L2 | コマ内の画像・フキダシもさらに個別レイヤーに分割 | 中〜高 | 実用的な編集が可能。ただしテキストは常にラスタライズ止まり |

### 調査2: PSD以外の外部編集可能フォーマット

| フォーマット | 実装コスト | テキスト編集 | ベクター図形編集 | 備考 |
|---|---|---|---|---|
| **SVG(生)** | ほぼゼロ | ◎ 本物の`<text>` | ◎ フキダシのpathそのまま | Illustrator/Inkscape/Affinityが標準対応 |
| ベクターPDF(新規) | 中 | ○ フォント埋め込みなら可 | ◎ | 印刷・配布ワークフロー向け |
| Krita `.kra` | 高 | △〜○ | ◎ 本物のベクターレイヤー | オープンでドキュメント化された形式。要新規実装 |
| CLIP STUDIO PAINT `.clip` | 非推奨 | 不明 | △ | 仕様非公開、信頼できる書き込み手段が実質無い |

**結論**: レイアウトタブは内部的に既にSVGそのもの（`buildMergedSvg()`が1ページ分の完全なSVGを生成済み、`embedFontsInSvg()`でフォント埋め込みまで既に実装済み＝現在は内部のラスタライズ用途にのみ使用）。PSDや新規フォーマットに変換するより、**このSVGをそのままダウンロードできるようにするのが最も費用対効果が高い**。テキストは本物の`<text>`、フキダシはpath、グラデーション・マスク・クリップパスもSVGネイティブなので、PSDでは不可能だった「外部ソフトで再編集可能」がそのまま実現する。

既存のPDF/EPUB書き出し(`13-export-pdf-epub.js`)は`buildMergedSvg()`→canvasラスタライズ→画像埋め込みという**閲覧用の完全フラット化**であり、編集用途には使えない点に注意（今回追加したSVG出力とは別物）。

## フェーズ1: 出力タブへのSVGエクスポート追加（実施済み・2026-09-07）

出力タブの「形式」セレクトに`SVG`を追加し、既存のPNG/JPEG/WebP出力と同じzip保存・フォルダ書き込み・保存ダイアログの経路を共有する形で実装した。

- `templates/index.html`: `#export-format`に`<option value="svg">SVG</option>`を追加（PDF/EPUBより前、画像系フォーマットのグループに配置）
- `static/js/main/12-text-png-export.js`:
  - `drawSvgOnCanvas()`からSVGのwidth/height属性設定ロジックを`_prepareSvgForExport(svgText, targetWidth, targetHeight)`として切り出し（viewBoxは維持するため出力サイズを変えてもベクター内容は劣化しない）
  - `handleExport()`に`isSvg`分岐を追加。SVG形式選択時はcanvasラスタライズをスキップし、「背景のみ」チェック時は`removeTextNodes()`適用後のSVG、通常時は`buildMergedSvg()`+`embedFontsInSvg()`適用後のSVGをそのまま`Blob(type:'image/svg+xml')`として書き出す
- メタデータ埋め込み（タイトル・著者等）はPNG/JPEG/WebP/PDF/EPUB限定のまま変更なし（SVGへの埋め込みは今回のスコープ外）

## 外部ソフトでの実地互換性検証結果（ユーザー確認・2026-09-07）

出力タブから書き出したSVGを実際に主要な外部ソフトで開いた結果、以下の互換性が確認された。**次回ヘルプ更新時にこの内容を反映すること**（現時点では未反映）。

| ソフト | 結果 |
|---|---|
| **Inkscape** | ○ ほぼ再編集可能な状態で開けることを確認 |
| **Affinity(Designer/Publisher)** | ○ ほぼ再編集可能な状態で開けることを確認（一部制限あり） |
| **Adobe Illustrator** | △ 画像のリンクが切れ、個別に再指定が必要。それ以外の要素（コマ枠・フキダシ形状等）はテンプレートとして開けるが、画像込みでの実用的な再編集は困難 |
| **CorelDraw** | ✕ 読み込み時にハングアップし使用不可 |

**ヘルプ記載時の想定文言の骨子**: 「出力タブのSVGエクスポートは、Inkscape・Affinity Designer/Publisherでの再編集を主な対象としています（画像・テキスト・フキダシ形状を含めほぼそのまま再編集可能）。Adobe Illustratorは画像リンクが切れるため個別に再設定が必要で実用性が低く、CorelDraw は読み込み時にハングアップするため非対応として扱ってください」という趣旨で、日英中3言語（[[comic-creator-workflow]]のヘルプ多言語同期の注意点を参照）に反映する。

## フェーズ2: 編集後SVGの再インポート機能（ユーザー判断により見送り・2026-09-07）

外部ソフトで編集したSVGを再度ページタブに取り込みたい、という要望について検討した論点（記録として残すが、着手はしない）:

- **ページ全体を1つのSVGとして再インポートし、自動的に元のコマ/オーバーレイ構造へ再分割する」は難易度が高い**。現在の分離は`data-clip-panel`/`data-overlay-layer`/`data-draft-layer`という属性ベースの規約に依存しているが、外部ツール（特にIllustrator）は保存時にグループ構造の入れ替えやID/属性の書き換えを行うことがあり、この規約が壊れて誤ったコマに内容が振り分けられるリスクがある。
- **フキダシのテキスト（`<text>`+`<tspan>`、縦書き対応・行間dataset等の独自属性）は外部ツールに理解されないアプリ固有のセマンティクス**なので、外部で編集されたテキストを再取込しても、縦書き設定や行間スタイル等のメタデータは失われ、プレーンな横書きテキストとして扱われる可能性が高い。
- 一方、**「1コマ分だけを個別SVGとしてエクスポート→そのコマ用に再インポートして`panelSvgContent`を丸ごと差し替える」という単位なら、既存の`importImageAsPage()`（画像1枚→1ページ）と同じ発想で、対象が明確なぶんリスクを大きく下げられる**。ユーザーが差し替え先のコマを明示的に選ぶため、属性ベースの自動判定が不要になる。

**推奨**: ページ全体の完全な往復編集（自動再分割）はまず狙わず、「コマ単位でのSVG差し替えインポート」を先に検証する方向を推奨する。ユーザーとの合意が取れ次第、設計・実装に着手する。

**Why:** PSD/新規バイナリ形式はpsd-tools等の書き込み制約により編集可能性に上限があるが、SVGは既に内部形式そのものであり追加コストなしに最高忠実度を得られるため。
**How to apply:** 今後「外部ソフトで編集したい」という要望が来たら、まずSVGエクスポート/インポートで対応できないか検討する。ページ全体の自動往復編集は属性依存で壊れやすいため、単位を小さく（コマ単位等）区切れないか設計すること。
